import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'tarjetasSupa',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
